source 'https://rubygems.org'

# Use GitHub version of Jekyll because the gem is way behind on things like URL templates
gem 'jekyll', :git => 'https://github.com/mojombo/jekyll.git'
gem 'jekyll-import'
gem 'hpricot'